# v1 (SQLite) vs v2 (mprcontents/)

Differences between the two MPR format versions: v1 storing all documents in a single SQLite database, and v2 splitting documents into individual files under an mprcontents/ directory.
